node_modules/
dist/
dist-electron/
release/

# 自动生成产物（图标 bundle，运行 pnpm generate-icons 重建）
src/generated/

# 测试文件
# test/ 下为手动自检/临时验证脚本（如动态代理自检），不入版本库
# 根目录扁平 test-*.py / test_*.ts 同为手动自检脚本，一并忽略
# src/**/*.test.ts 是 vitest 正式单测，仍入版本库
test/
test-*.py
test_*.ts
test_screenshots/

.claude/
pnpm-lock.yaml

repo-docs/

# 本地环境变量（含 Cloudflare token 等敏感值，不入版本库）
.env
.env.local
.env.*.local